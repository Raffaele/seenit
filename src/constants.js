const constants = {
  // This constant (PERMISSION_EMAIL) should be in a .env file, never committed
  // I'll keep it as constant here just because it's a test 
  PERMISSION_EMAIL: 'mori.raffaele@gmail.com',
  API_END_POINT: 'https://tech-test-service-staging.seenit.studio/v1/uploads',
  ITEM_PER_REQUEST: 30
};

export default constants;